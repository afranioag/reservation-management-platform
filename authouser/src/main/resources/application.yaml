server:
  port: 8091

spring:
  application:
    name: authuser-service

eureka:
  client:
    serviceUrl:
      defaultZone: 'http://localhost:8090/eureka'
  instance:
    hostname: localhost

reservation:
  api:
    url:
      notification: 'http://notification-service/notifications'
