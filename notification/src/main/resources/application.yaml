server:
  port: 8092

spring:
  application:
    name: notification-service

eureka:
  client:
    serviceUrl:
      defaultZone: 'http://localhost:8090/eureka'
  instance:
    hostname: localhost

reservation:
  api:
    url:
      authuser: 'http://authuser-service/authusers'